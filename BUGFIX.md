# 书声 (SoundBook) 故障修复与工程经验沉淀 (BUGFIX)

本文档为“书声”项目的长期集中维护文档，用于统一记录系统开发与演进过程中遇到的重要专项 Bug 修复与核心技术攻关结论。

---

## 维护规范与规则

后续若出现新的系统级或专项 Bug（例如：字幕时间轴错位、BGM 自动混音异常、断点续跑 Resume 失败、视频硬字幕压制错误、TTS 新模型适配异常等），修复完成后统一在本文件中以 **专项章节** 形式追加记录，禁止在项目各处创建零散的临时修复说明。

每个专项必须包含以下标准结构：
1. **问题现象**：清晰描述异常复现场景、波及范围与用户体感；
2. **排查过程**：简洁记录关键对照实验与排除逻辑（拒绝粘贴无关冗余对话，沉淀核心事实）；
3. **根因结论**：阐明深层物理机理与架构影响，明确是否涉及底层改动；
4. **固化参数**：列出最终验证通过的稳定生产配置与边界值；
5. **最小回归测试**：标明测试集路径、测试用例、验收标准与重跑命令；
6. **修改历史**：列出涉及的代码文件、配置变更及对应的 Git Commit Hash。

---

## 专项一：F5-TTS 句首漏词与变音异常专项

### 1. 问题现象
* 在使用 F5-TTS (DiT 流匹配架构) 合成有声书正文时，音频开头频繁出现句首词丢失（如“如果”、“就算”、“反之”等词首字或全词完全未发音）、吞字或抢字；
* 在原版 26 字硬切分策略下，该现象呈系统性放大（单段音频内高达 8 处漏词）；
* 句首漏词出现在固定语句位置，严重影响听觉体验与书籍出版质量。

### 2. 排查过程（单变量实验法）
1. **切分策略对照**：关闭 26 字硬切，切换为自然标点切分，句首漏词从 8 处显著收敛至 4 处固定位置。证实 26 字硬切严重破坏语义，是问题的放大器；
2. **后处理环节排除**：
   * 关闭 `trim_audio_silence`（自适应能量静音剥离），漏词依然存在，排除首部静音误剪；
   * 关闭 `fade_in`（10ms 防爆音微淡入），漏词依然存在，排除淡入压制；
3. **推理链路纯度验证**：绕过 Worker 进程交互协议与音频拼接逻辑，直接调用官方 F5 内存推理生成原始 WAV，4 处漏词在模型直接输出时已客观存在，排除业务管道污染；
4. **生成超参数探索**：
   * 更换随机种子（Seed=142）：漏词从 4 处变为 3 处，但剩余位置仍稳定丢失，证实 Seed 具有一定扰动性，但并非根因；
   * 提高推理步数（NFE 从 16 提高至 32）：漏词重新回到固定的 4 处，GPU 耗时成倍增加，证实 NFE 步数不足不是根因；
5. **声学截断与参考音文对齐深度定位**：
   * 提取 F5 裁切边界前未截断的完整音频（`pre_crop`），发现每句开头均残留前置参考音频；
   * 仔细听验原预置参考音频 `preset_male_e1_narrator.wav`（6.80s），发现尾部实际多读了“，与此同时”，而原参考文本 `.txt` 中仅写到“……持续加码。”；
   * 经声学能谱分析，原音频在 5.20s~5.80s 存在长达 600ms 的完全静音，随后 5.90s~6.60s 念“与此同时”；
   * 当参考文本漏写“与此同时”时，扩散模型按字数预估的时长与实际波形严重冲突，导致时间轴对齐向右发生系统性漂移，F5 原生截断线切入正文开头，吞掉了句首词；
6. **尾部闭合与音文绝对对齐**：
   * 将参考音频在 5.30s 静音低谷处物理截断（保留“持续加码”完整发音，切除“与此同时”）；
   * 将参考文本同步清理，去除未实际发音的“例如”，保持与 5.30s 音频 100% 逐字对应；
   * 重新生成后经人工严格听验：**0 漏词、0 多字、语调自然、发音清晰，测试完美通过！**

### 3. 根因结论
1. **根本物理诱因**：Reference Audio 与 Reference Text 存在音文错位（开头多字、结尾音频未完结且文字缺失）。F5-TTS 作为基于 Prefix 条件的流匹配模型，音文错位会导致隐空间注意力时间轴发生严重对齐漂移，使得物理截断线切入生成内容内部；
2. **问题放大机制**：26 字硬切分在逗号处截断，强行制造不符合自然语调的超短从句，破坏了语流连续性，放大了对齐漂移带来的丢字风险；
3. **底层逻辑裁定**：没有证据表明 F5 官方 `ref_audio_len` 截断逻辑存在缺陷。在 Reference Pair 达成 100% 精确对齐的前提下，原生截断逻辑表现极其稳定，严禁擅自修改官方算法底层。

### 4. 固化生产参数
* **TTS 模型**：`SWivid/F5-TTS` (权重文件：`F5TTS_v1_Base/model_1250000.safetensors`)
* **Reference Audio**：`models/f5_tts/presets/preset_male_e1_narrator.wav`（5.300 秒，24000Hz 单声道，切除尾部未完结词）
* **Reference Text**：`大幅增加短期美债持有量和对日本五大商社的持续加码。`（与音频 100% 逐字对应）
* **Speed**：`1.0`
* **NFE 步数**：`16`（稳定且兼顾推理性能）
* **Seed 策略**：`42 + sent_idx`（保证可复现性与句间微扰动自然度）
* **文本切分策略**：自然句切分 `split_natural_sentences`（依据 `。！？!?；;\n` 切分，严禁 26 字硬切）
* **官方截断逻辑**：保持官方原生 `ref_audio_len`，不增设额外补丁

### 5. 最小回归测试集
* **测试代码路径**：`tests/regression/test_f5_minimal_regression.py`
* **测试单元清单**：
  1. `我认为股市整体价格高于内在价值，主要是蓝筹股估值过高。`
  2. `如果确实如此，所有股票都存在大幅下跌的风险，无论是否低估。`（关键核验：句首“如果”）
  3. `无论如何，我认为，五年之后回过头来看，人们不太可能觉得现在的价格很便宜。`
  4. `就算大规模熊市出现，我们的套利类部分投资的市场价值也不会受到影响。`（关键核验：句首“就算”）
  5. `如果整个市场回归到低估状态，我们可能会把所有资金都投入到低估类中，可能还会借一部分钱来买低估的股票。`（关键核验：句首“如果”）
  6. `反之，假如市场继续大幅走高，我们的策略是，将低估类不断获利了结，并增加套利类投资组合的比重。`（关键核验：句首“反之”）
* **验收门禁指标**：
  - 6 个测试单元全部生成成功；
  - 0 句首漏词、0 凭空多字；
  - 整体语调平稳自然，无爆音与电音截断。
* **执行命令**：
  ```bash
  pytest -v tests/regression/test_f5_minimal_regression.py
  ```

### 6. 修改历史与版本提交
* **变更文件清单**：
  - `models/f5_tts/presets/preset_male_e1_narrator.wav`（替换为 5.30s 干净剪辑音频）
  - `models/f5_tts/presets/preset_male_e1_narrator.txt`（更新为 100% 逐字对应文本）
  - `config.yaml`（同步固化音色文本与全局 ref_text）
  - `workers/f5_worker.py`（废弃 26 字硬切，默认采用自然句切分，固化 NFE=16 与 Seed 递增策略）
  - `tests/regression/test_f5_minimal_regression.py`（新增最小回归测试集）
  - `BUGFIX.md`（新增项目长期集中维护文档）
  - `docs/01_PRD.md`、`docs/02_ARCHITECTURE.md`、`docs/03_DETAILED_DESIGN.md`（同步更新设计与规范）
* **对应 Git Commit Hash**：`65ceb07` (Branch: `main`)
